export enum WEBSOCKET_SERVER_EVENTS {
  PING = "ping",
  PONG = "pong",
  CONNECTION_ESTABLISHED = "conn",
  CHAT = "chat",
  STREAM = "stream",
  END_OF_STREAM = "eos",
  SYNC = "sync",
  ERROR = "err",
}
